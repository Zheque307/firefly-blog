import type { CommentConfig } from "../types/commentConfig";

export const commentConfig: CommentConfig = {
	// 评论系统类型: none, twikoo, waline, giscus, disqus, artalk
	// 本站不启用任何评论系统（设为 none 时，全站不渲染评论区）
	type: "none",
};
