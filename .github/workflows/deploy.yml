name: Build and Deploy to Aliyun

on:
  push:
    branches: [main]
    # 只改说明文档时不触发构建
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/workflows/**"
  workflow_dispatch:

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  build-and-deploy:
    name: 构建并部署
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 拉取源码
        uses: actions/checkout@v4

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.22.0

      - name: 安装 Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: 安装依赖
        run: pnpm install --no-frozen-lockfile

      - name: 构建站点
        run: pnpm build
        env:
          NODE_OPTIONS: --max-old-space-size=4096

      - name: 检查构建产物
        run: |
          echo "文件数: $(find dist -type f | wc -l)"
          du -sh dist
          ls dist | head -20

      - name: 检查 Secrets 是否配置
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          fail=0
          for name in VPS_HOST VPS_USER VPS_SSH_KEY; do
            val="${!name}"
            if [ -z "$val" ]; then
              echo "::error::Secret $name 为空或未配置（请在仓库 Settings → Secrets and variables → Actions 中添加）"
              fail=1
            else
              echo "$name 已配置，长度 ${#val}"
            fi
          done
          [ "$fail" -eq 0 ] || exit 1

      - name: 配置 SSH
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          # 自动兼容两种 Secret 格式：
          #   A) base64 编码后的私钥（本项目最初提供的方式）
          #   B) 直接粘贴的原始私钥文本（含 BEGIN/END 行）
          if printf '%s' "$VPS_SSH_KEY" | grep -q "BEGIN OPENSSH PRIVATE KEY"; then
            echo "检测到原始私钥文本格式，直接写入"
            printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/id_deploy
          else
            echo "按 base64 解码私钥"
            if ! printf '%s' "$VPS_SSH_KEY" | tr -d '\n\r ' | base64 -d > ~/.ssh/id_deploy 2>/dev/null; then
              echo "::error::VPS_SSH_KEY 既不是合法 base64，也不含 'BEGIN OPENSSH PRIVATE KEY'"
              echo "::error::请确认 Secret 内容：应为「私钥文件的 base64 编码」或「私钥文件原文」"
              exit 1
            fi
          fi

          chmod 600 ~/.ssh/id_deploy

          if ! grep -q "PRIVATE KEY" ~/.ssh/id_deploy; then
            echo "::error::解码后不是有效的私钥，请重新复制 Secret 内容（注意不要漏字符、不要带多余空格）"
            head -c 60 ~/.ssh/id_deploy
            exit 1
          fi
          echo "私钥格式校验通过（$(head -1 ~/.ssh/id_deploy)）"

          if [ -z "${VPS_HOST:-}" ]; then
            echo "::error::VPS_HOST 为空"
            exit 1
          fi
          ssh-keyscan -H "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          chmod 600 ~/.ssh/known_hosts
          echo "known_hosts 条目数: $(wc -l < ~/.ssh/known_hosts)"

      # 注意：部署密钥在服务器端是「强制命令」模式 —— 只允许 rsync，
      # 任何其它命令（包括 echo 探活）都会被拒绝。因此这里不做独立连通性测试，
      # 直接由下面的 rsync 步骤充当连通性验证（失败时会报出具体错误）。
      - name: 上传构建产物到服务器
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes" \
            dist/ "$VPS_USER@$VPS_HOST:/var/www/firefly/staging/"

      - name: 原子发布并验证
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          ADMIN_SSH_KEY: ${{ secrets.ADMIN_SSH_KEY }}
        run: |
          # 发布脚本是任意命令，受限的部署密钥（forced command）无法执行，
          # 因此这里使用主管理密钥 ADMIN_SSH_KEY（base64 编码后存入 Secret）。
          # 若未配置，则仅上传产物到 staging，由服务器端或人工执行发布脚本。
          if [ -z "$ADMIN_SSH_KEY" ]; then
            echo "::notice::未配置 ADMIN_SSH_KEY，跳过自动发布。产物已在 /var/www/firefly/staging/，请在服务器执行 dsh-publish.sh"
            exit 0
          fi
          printf '%s' "$ADMIN_SSH_KEY" | tr -d '\n\r ' | base64 -d > ~/.ssh/id_admin
          chmod 600 ~/.ssh/id_admin
          ssh -i ~/.ssh/id_admin -o StrictHostKeyChecking=yes -o ConnectTimeout=15 \
            "$VPS_USER@$VPS_HOST" "/usr/local/bin/dsh-publish.sh"

      - name: 部署结果
        run: echo "✅ 部署流程完成"
